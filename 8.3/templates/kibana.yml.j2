# Warning: This file is Ansible Managed, manual changes will be overwritten on next playbook run.
#

server.port: {{ elk_kbn_port }}
server.host: "0.0.0.0"
elasticsearch.hosts: [{% for es_host in elk_es_addresses-%}"http://{{ es_host }}:{{ elk_es_port }}{{ '", ' if not loop.last else '"' }}{%- endfor %}]
kibana.index: ".kibana"
